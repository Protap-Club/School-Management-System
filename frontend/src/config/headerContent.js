// Header branding configuration
// Change the logo path or set to null to use the initials fallback
export const headerContent = {
    logo: null, // e.g. '/logo.png' — set to null to use school initials
};
